import { Button } from '@antd-taro';
import { View } from '@tarojs/components';
import './index.less';

const IndexPage = () => {
  return (
    <View>
      <Button>按钮</Button>
    </View>
  );
};

export default IndexPage;
